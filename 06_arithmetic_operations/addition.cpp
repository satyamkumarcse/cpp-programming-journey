#include <iostream>
int main(){

    int students = 20;

    // students += 1;
    // students = students + 1;
    students++;  // if we need to only add 1, we can use the increment operator (++)

    std::cout<<students;




    return 0;
}