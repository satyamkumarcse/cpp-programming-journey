// most of the math function are found inside the cmath header file.


#include <iostream>
#include<cmath>
int main(){

    // double x = 3;
    double x = 3.14;
    double y = 4;
    double z;

    // z =  std::max(x,y); (these 2 functions, {max and min} can be used without including cmath header file also.)
    // z =  std::min(x,y);

    // 1) exponents

    // z = pow(2,5);
    


    // 2) square root

    // z = sqrt(121);


    // 3) abs - absolute value (makes negative into positive {modulus} )

    // z = abs(-5); 


    //  4) rounding off.... (round)

    // z = round(x);

    // z = ceil(x); 
    // z = floor(x);




    std::cout<<z;
    return 0;
}


