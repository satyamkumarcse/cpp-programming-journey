/*namespace = provides a solution for preventing name conflicts in large projects. each entity needs a unique name. a namespace allows for identically name entities as long as the namespaces are different.*/

#include <iostream>

int main(){

    /*
    int x=0;
    int x=1;
    here we will get an error that, the variable was re-declared. 
    but if we use namespaces, then two or more entities can share same name.
    we can declare any number of name space
    */
    

    return 0;
}